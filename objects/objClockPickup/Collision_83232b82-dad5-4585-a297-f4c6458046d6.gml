if(world.gameState == "play"){
	
	//with(objMob){ stunTime += 300; }
	//world.spawnCD += 300;
	//with(objMobComing){ stunTime += 300; }
	//with(objHydrant){ shotCD += 300; }
	//with(objZapBlock){ zapCD += 300; }
	//with(objGeyser){ stunTime += 300; }
	
	//with(objEnemyShot){ stunTime += 300; }
	//with(objShotComing){ stunTime += 300; }
	//with(objWaterShot){ stunTime += 300; }
	
	//world.fadeBlockTime += 300;
	
	event_inherited();
	
}